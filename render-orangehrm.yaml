services:
  - type: web
    name: orangehrm-app
    runtime: docker
    repo: https://github.com/victor-render2026/orangehrm-render
    branch: main
    dockerfilePath: Dockerfile.render
    plan: free
    envVars:
      - key: PORT
        value: "80"
    autoDeploy: true